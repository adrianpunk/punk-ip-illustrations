---
name: punk-ip-article-illustrations
description: 读取完整文章、Markdown、网页、帖子、文章链接或单个观点，使用内置 Punk 个人 IP 直接生成复古彩色扁平 3D 小剧场风格文章插图，并按信息密度自动决定数量和插入位置。适用于文章配图、正文插图、长文多图、Punk IP 配图、editorial illustration、flat 3D illustration 等任务。
---

# Punk IP 文章插图

## 开始前

完整读取以下文件：

- `references/character-spec.md`：应用内置 Punk 的固定人物身份。
- `references/illustration-style.md`：应用复古彩色扁平 3D IP 小剧场视觉规范。
- `references/article-workflow.md`：确定图片数量、认知锚点、生成、保存和交付方式。

把 `assets/punk-character-sheet.png` 作为人物身份的唯一正式视觉参考。设定图中的主形象与四视图是同一角色的不同角度，不是多名角色。

开始生图前，用中间过程提示：`当前使用 IP：Punk (punk)`。

Punk 是人物身份，不是文章画风。文章画风固定使用 `punk-retro-flat-3d-theater`。

## 执行工作流

1. 读取完整文章或用户提供的全部文字。用户提供文章地址时获取完整正文，不根据标题单独推测内容。
2. 提炼核心观点、关键转折、文章情绪和真正值得视觉化的认知锚点。
3. 根据正文长度和信息密度决定图片数量，合并重复内容，不按章节平均覆盖。
4. 为每张图确定一个核心判断、一个物理关系、适合内容的表现模式和 1—3 个主要物件。
5. 安排 Punk 的叙事职责、人物尺度、动作和适合当前内容的情绪。
6. 列出少量必须准确呈现的短文字；文字语言跟随输入文章。
7. 在内部完成整组图片方案，不先输出方案，不等待二次确认。
8. 把 Punk 人物设定图作为同一角色的多角度参考，逐张独立生成，不用拼图代替。
9. 逐张检查文章含义、文字准确性、人物一致性和画面密度；每次只修正一个主要问题。
10. 按输出规则保存图片。最终回复保持简短，按顺序列出每张图片、用途、绝对路径和建议插入位置。

## 角色与风格边界

- 人物规范只定义 Punk 的身份、服装、配件、身体比例和稳定色。
- 画风规范只定义概念提炼、表现模式、物件关系、构图、颜色、文字和留白。
- 不因文章主题改变 Punk 的固定身份，也不因人物特征改变文章分析、图片数量、认知锚点、保存和交付规则。
- 让 Punk 参与核心关系，但不要让人物压过文章观点。
- 不使用原始临时上传路径替代 Skill 内的正式人物设定图。
- 不把一次生成中的偶然风格漂移写回人物规范。

## 输出边界

- 文章插图优先保存到当前项目根目录的 `.punk-ip-assets/<article-slug>/`。
- 无项目、项目不可写或宿主沙箱禁止写入时，回退到 `~/.punk-ip-assets/<article-slug>/`。
- 不覆盖已有图片；使用 `-v2`、`-v3` 递增版本。
- `.punk-ip-assets/` 只保存文章插图，不保存人物设定图、角色规范或文章副本。
- 默认把输入文章视为只读，只在最终回复中简要提供每张图片的插入位置。
- 只有用户明确要求把图片引用写入文章时，才修改原文章。

## 工具降级

宿主没有图像生成能力时，输出每张图的完整可执行提示、人物参考图绝对路径和保存计划，并明确标注图片尚未生成。不要伪造图片文件。
