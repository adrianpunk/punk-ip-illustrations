---
name: punk-ip-article-illustrations
description: 使用内置 Punk 个人 IP 角色设定图，将中文或其他语言的主题、观点、段落、帖子、文章内容转换为横版 16:9 的“复古彩色扁平 3D IP 小剧场”长文正文插图。适用于用户要求 Punk IP 配图、个人 IP 插图、长文插图、复古扁平 3D 编辑插画、小剧场插图，或指定“流程拆解 / 核心动作 / 自动判断”模式生图的任务。
---

# Punk IP 长文插图

## 开始前

完整读取以下文件：

- `references/character-spec.md`：固定人物身份与一致性边界。
- `references/visual-style.md`：画幅、构图、颜色、物件与文字规范。
- `references/content-workflow.md`：模式判断、内容压缩、生成、检查与交付流程。

把 `assets/punk-character-sheet.png` 作为人物身份的唯一正式视觉参考。每次生成或修订都向图像生成工具传入该图片；不要只依赖文字复述，也不要使用用户原始临时路径。

## 执行工作流

1. 完整读取用户提供的主题、观点、段落、帖子或文章内容。缺少实际内容时，只追问内容本身。
2. 解析用户指定模式：`流程拆解`、`核心动作` 或 `自动判断`。没有指定时按 `自动判断` 执行。
3. 按 `references/content-workflow.md` 提炼一个最重要的结论，并在内部完成画面方案；不要逐句翻译，不要先展示方案等待确认。
4. 按选定模式组织人物、动作、物件、结果和少量必要文字。
5. 组合人物规范与视觉规范，直接调用可用的图像生成工具，生成一张独立的 16:9 插图。
6. 对照三个参考文件逐项检查。若存在明显问题，围绕一个主要问题进行编辑或重生成，并继续使用人物设定图作为参考。
7. 保存最终图片且不覆盖已有文件；展示图片，并简要报告所选模式、表达意图、绝对路径和建议插入位置。

## 不可妥协项

- 保持 Punk 人物的脸部比例、发型、墨镜、服装、鞋、手表、身体比例和整体气质一致。
- 背景必须是纯白页面；不使用米黄底、渐变、纹理底或完整室内外空间。
- 画幅固定为横版 16:9；主体小剧场约占画面 55%—65%，居中且四周保留明显留白。
- 只制作长文正文插图；不做封面、宣传海报、PPT 信息图或大标题版式。
- 保证 3 秒内能理解核心内容；删掉不服务于结论的文字、装饰、节点和物件。

## 工具降级

当前宿主没有图像生成能力时，输出一份可直接执行的完整生图提示词，明确列出人物参考图绝对路径、选定模式、需要逐字呈现的文字和保存计划，并说明图片尚未生成。不要伪造图片文件。
